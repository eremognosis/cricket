name: "dbtcrick"
version: "1.0.0"
config-version: 2

profile: "dbtcrick"

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
analysis-paths: ["analyses"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"
  - "logs"

models:
  dbtcrick:
    staging:
      +materialized: view
    inter:
      +materialized: view
    mart:
      +materialized: table
